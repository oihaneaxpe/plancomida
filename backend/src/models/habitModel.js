const db = require('../database');

class HabitModel {
  // static getAllRecipes(callback) {
  //   db.query('SELECT * FROM tmcategoria', (err, results) => {
  //     if (err) {
  //       callback(err, null);
  //       return;
  //     }
  //     console.log("aa")
  //     console.log(results);

  //     callback(null, results);
  //   });
  // }

  // static addRecipe(recipeData, callback) {
  //   db.query('INSERT INTO tmreceta SET ?', recipeData, (err, result) => {
  //     if (err) {
  //       callback(err, null);
  //       return;
  //     }
  //     callback(null, result);
  //   });
  // }
}

module.exports = HabitModel;
